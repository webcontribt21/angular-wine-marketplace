import { ValueDescriptionPipe } from './value-description.pipe';

describe('ValueDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
