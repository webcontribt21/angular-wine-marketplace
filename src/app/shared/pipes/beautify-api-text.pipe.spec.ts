import { BeautifyApiTextPipe } from './beautify-api-text.pipe';

describe('BeautifyApiTextPipe', () => {
  it('create an instance', () => {
    const pipe = new BeautifyApiTextPipe();
    expect(pipe).toBeTruthy();
  });
});
