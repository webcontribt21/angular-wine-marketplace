import { ResizedEventDirective } from './resized-event.directive';

describe('ResizedEventDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizedEventDirective();
    expect(directive).toBeTruthy();
  });
});
