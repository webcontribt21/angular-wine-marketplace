import { HorizontalMoveDirective } from './horizontal-move.directive';

describe('HorizontalMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new HorizontalMoveDirective();
    expect(directive).toBeTruthy();
  });
});
